/** Checkout shares the address form's field styling so the whole page reads
 *  as one design system. Re-exported under checkout-local names. */
export {
  fieldCard as cardClass,
  fieldLabel as labelClass,
  fieldControl as controlClass,
  fieldIcon as iconClass,
  fieldDisabled as disabledClass,
} from "@/components/address/styles";
